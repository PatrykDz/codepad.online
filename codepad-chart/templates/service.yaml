apiVersion: v1
kind: Service
metadata:
  name: {{ include "codepad-chart.fullname" . }}
  labels:
    {{- include "codepad-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codepad-chart.selectorLabels" . | nindent 4 }}
