import {COLORS} from "./colors";

const Theme = {
    borderRadius: {
        default: '10px'
    },
    font: {
      default: {
          fontFamily: 'Lato, sans-serif', //'Segoe UI',
          fontWeight: 700
      }
    },
    colors: COLORS
}

export default Theme